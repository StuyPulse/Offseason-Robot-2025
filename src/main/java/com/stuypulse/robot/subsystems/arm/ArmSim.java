package com.stuypulse.robot.subsystems.arm;

public class ArmSim {

    
    for (int i = 0; i < obstaclePoints.size(); i++) {
        obstaclePoints.get(i).getFirst();
        obstaclePoints.get(i).getSecond();
    }
}
